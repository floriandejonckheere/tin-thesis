# frozen_string_literal: true

RSpec.describe 'MongoDB' do
  context 'querying' do
    describe 'Select N most recent events, ordered reverse chronologically'

    describe 'Select N most recent events, where the event is related to a topic in a list of given topics, ordered reverse chronologically'

    describe 'Select N most recent events, where the event is related to a given topic, ordered reverse chronologically'

    describe 'Select N most recent events, where the event is related to a given user, ordered reverse chronologically'
  end

  context 'inserting' do
    describe 'Insert N given events'
  end
end
